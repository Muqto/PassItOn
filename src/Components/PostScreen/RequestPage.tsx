import React, { useState } from "react";
import { View, Text } from "react-native";
import { TextInput } from "react-native-paper";
import styles from "./Styles";

const RequestPage = () => {
  return (
    <View>
      <Text>REQUEST PAGE HERE</Text>
    </View>
  );
};

export default RequestPage;
